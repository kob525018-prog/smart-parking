#include <stdio.h>

int main(){
    float a = 20, b = 30,z,h,x,y;
    z = a + b; 
    h = a - b;
    x = a * b;
    y = a / b;
    printf("a + b = %f\n", z);
    printf("a - b = %f\n", h);
    printf("a * b = %f\n", x);
    printf("a / b = %f\n", y);
    return 0;
}